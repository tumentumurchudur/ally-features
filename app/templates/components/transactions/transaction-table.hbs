<table class="transactions-table">
  <thead>
    <tr class="header-row">
      {{#each headers as |header|}}
        {{transactions/transaction-table-header header=header}}
      {{/each}}
    </tr>
  </thead>
  <tbody>
    {{#each rows as |row|}}
      {{transactions/transaction-table-row row=row}}
    {{/each}}
  </tbody>
</table>
