<div class="add-transaction">
  <div class="title">Add a Transaction</div>
  <label>Amount</label>
  {{input type="number" class="input-amount" value=amount}}

  <label>Description</label>
  {{input class="input-description" value=description}}

  <label>Date</label>
  {{pikaday-input
    format='MMM DD, YYYY'
    value=date
    onSelection=(action "selectDate")
    class="datepicker"}}

  <label>Category</label>
  <select id="ddCategory" onchange={{action "selectCategory" value="target.value"}}>
    <option value="">Select category</option>
    {{#each categories as |category|}}
      <option value={{category.name}}>{{category.name}}</option>
    {{/each}}
  </select>

  <label>Account</label>
  <select id="ddAccount" onchange={{action "selectAccount" value="target.value"}}>
    <option value="">Select account</option>
    {{#each accounts as |account|}}
      <option value={{account.name}}>{{account.name}}</option>
    {{/each}}
  </select>

  <label>NOTE TO SELF</label>
  {{textarea rows="2" maxlength="40" value=memo key-up=(action "changeTextArea")}}

  <div class="buttons">
    {{bulma-button class="add-button" disabled=enableSave isSmall=true isInfo=true label="Add" onmousedown=(action "add")}}
    {{bulma-button label="Cancel" isSmall=true onmousedown=(action "close")}}
  </div>
  <div>{{remainingChars}} characters left.</div>
</div>
