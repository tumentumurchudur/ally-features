{{#transactions/transaction-filter
  filter=(route-action "filter")
  getAccounts=(route-action "getAccounts")
  getCategories=(route-action "getCategories")
  addTransaction=(route-action "addTransaction")
  filterByDateRange=(route-action "filterByDateRange") as |rows|}}
    {{transactions/transaction-table
      showTotal=true
      showTotalLabel="Total Amount"
      rows=rows}}
{{/transactions/transaction-filter}}
