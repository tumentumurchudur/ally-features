
import { getSortIndicator } from 'ally-transaction/helpers/get-sort-indicator';
import { module, test } from 'qunit';

module('Unit | Helper | get sort indicator');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getSortIndicator([42]);
  assert.ok(result);
});

